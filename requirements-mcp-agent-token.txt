# Requirements for MCP Server + LiveKit Agent + Token Server
# Files: mcp_server.py + agent.py + token_server.py

# Core Python dependencies
python-dotenv>=1.0.0

# HTTP client for MCP communication
httpx>=0.25.0

# MCP Server (FastAPI)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# LiveKit Agent dependencies
livekit>=0.8.0
livekit-agents>=0.8.0
livekit-plugins-openai>=0.8.0
livekit-plugins-silero>=0.8.0

# Token Server (Flask)
flask>=2.3.0
flask-cors>=4.0.0

# AI service for agent
openai>=1.3.0

# Audio processing for Silero VAD
torch>=2.1.0
torchaudio>=2.1.0
numpy>=1.24.0

# Email integration (optional - for Composio in MCP server)
composio>=0.3.0

# Additional utilities
pydantic>=2.5.0
python-multipart>=0.0.6